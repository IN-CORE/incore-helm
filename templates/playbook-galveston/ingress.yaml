{{- if .Values.playbook_galveston.enabled -}}
{{- if .Values.ingress.enabled -}}
  {{- $fullName := include "incore.fullname" . -}}
  {{- $svcPort := .Values.playbook.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-playbook-galveston
  labels:
    {{- include "incore.labels" . | nindent 4 }}
  annotations:
    {{- include "incore.authIngressAnnotation" . | nindent 4 }}
spec:
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          - path: /playbook/galveston/
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ $fullName }}-playbook-galveston
                port:
                  number: {{ $svcPort }}
    {{- end }}
  {{- end }}
{{- end }}
