{{- if .Values.playbook_slc.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "incore.fullname" . }}-playbook-slc
  labels:
    {{- include "incore.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.playbook_slc.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "incore.name" . }}-playbook-slc
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "incore.name" . }}-playbook-slc
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: playbook-slc
          image: "{{ .Values.image.project }}/{{ .Values.playbook_slc.image.repository }}:{{ .Values.playbook.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.playbook.image.pullPolicy | default .Values.image.pullPolicy }}
          env:
            - name: DEPLOY_ENV
              value: {{ .Values.playbook_slc.env.deploy_env | quote }}
          ports:
            - containerPort: 80
              name: http
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.playbook_slc.resources | nindent 12 }}
      {{- with .Values.playbook_slc.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.playbook_slc.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.playbook_slc.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}